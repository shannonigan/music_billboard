<h1> Artists </h1>

<%= link_to 'Add a New Artist', new_artist_path %>
 <%# link_to 'View Songs', artist_songs_path(@artist) %>
<br />

<%= @artist.length === 0 ? "No Artists Added" : "" %>
<ul>
  <% @artist.each do |artist| %>
    <li> <%= link_to artist.name, artist_path(artist) %> </li>
  <% end %>